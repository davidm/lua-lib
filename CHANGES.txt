0.2.0.20120426
  A '<bin>' prefixed to a directory string will be replaced with the directory
    of the currently executing script (requires 'findbin' module).
  Add more options to `newrequire` and `LIB ()` functions.
  append and prepend renamed after and before repectively.
  Raise error if directory contains '?' or ';' character (unescapable).
  Rename LIB.path/cpath -> LIB.path_form/cpath_form.
  Add findbin dependency (optional but recommended).

0.1.1.20120407
  Repackage and reorganize sources.

0.1.20111203
  iniitial version
